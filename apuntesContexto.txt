contexto:
1. crear contexto con createContext
2. crear el provieder con los children
- aquí hacemos las funciones que pide el ejercicio
- luego usar el value para exportar todo
3. Para usar el contexto creamos el hook. Ej.:
// export default function useFamily():FamilyContextType{
//     const context = useContext(FamilyContext)
//     if(!context){
//         throw new Error("useFamily debe usarse dentro de un FamilyProvider")
//     }
//     return context
// }